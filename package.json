{
  "name": "@twobots/game-kit",
  "version": "0.3.0",
  "description": "Shared non-visual logic for the twobots.dev card-game lineup — a seeded bot-naming pool/picker, a bot thinking-delay picker, feedback-sending primitives, and storage primitives. Ships raw source; consuming apps compile it with their own TypeScript pipeline.",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bjornbasar/datvi.git"
  },
  "files": [
    "src"
  ],
  "exports": {
    "./names": "./src/names/index.ts",
    "./timing": "./src/timing/index.ts",
    "./feedback": "./src/feedback/index.ts",
    "./storage": "./src/storage/index.ts"
  },
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "docs-check": "node tools/check-docs.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
